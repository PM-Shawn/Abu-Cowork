// Execute the private organization-plugin UI test with the host app's React,
// shared toolbox components and Vitest aliases — the same boundary a
// production enterprise renderer build resolves.
import '@enterprise-modules/components/EnterprisePluginTab.test'
