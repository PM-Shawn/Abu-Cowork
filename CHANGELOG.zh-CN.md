# 更新日志

> 本文件是**中文** canonical 更新日志 —— 驱动 App 内更新弹窗与官网的中文展示。英文版见 [`CHANGELOG.md`](./CHANGELOG.md)（驱动 GitHub Release）。每次发版两份同步维护、语言不混（见 `RELEASING.md`）。v0.31.0 之前的历史仅英文版有。

## 未发布

### ✨ 新增

- **插件：一次装好一组技能、连接器和专家，也能自己做一个**：侧栏的「工具箱」改为「扩展」，分「插件」「技能」「连接器」三页，每页再分「市场」「我的」。通过「添加插件市场」接入电脑上已有的市场目录（为 Claude Code、Codex 打包的插件也能读）；安装前会列出插件要加进来的每个技能、连接器和专家，以及阿布暂不使用的内容，装好的插件可以停用、有新版本时更新，卸载时它带来的东西一并移除。想自己做，点「创建插件」，在对话里描述要解决的工作——阿布负责写出来，「校验并预览」让你看清将要安装的内容，之后改了源码，也要预览并确认更新才会替换已安装的版本。点错建出来的草稿可以在「我的」里删掉（源码文件夹会保留）；插件操作记录读不出来时，插件页会列出旧版本的备份位置并提供「归档并继续」，不删除任何文件，也不再让安装、更新、卸载全部停摆。自带界面的连接器，现在可以把界面直接显示在对话里。
- **团队：一个队长带几名队员，在一个对话里分工干活**：侧栏新增「团队」，把一名队长和几名队员组成团队；在对话里 @团队 派活，你只和队长沟通，由队长拆解任务、并行派给队员、复核结果再向你汇报。对话旁的「团队」面板显示每位队员的状态和每一次派活，进行中实时可看、结束后可回放，也能单独停止某位队员这次的活或给它「追加指令」；打开「分工先经我确认」，队长拆完分工会先停下等你点头再开工。队员不会卡在弹窗上：需要你拍板的事汇总在「需要你确认」条里，批准后这一步重新补跑，浏览器请求还可以直接「以后都允许该网站」（高风险页面和脚本类操作不提供）。每次运行最多派活 40 次，连续 3 次没完成的队员本次不再派活，15 分钟没有新动作的派活自动停止，被应用重启打断的运行会自己接着做，定时任务也可以「交给团队」。无论你自己编辑、改名还是让阿布保存，专家都会留在原来的团队里；专家被删除或改动导致失效的队员会标为「已失效」并可「移除」，不再被悄悄跳过，删除正被团队使用的专家前也会先确认。
- **定时任务、触发器和 IM 可以用浏览器了，需要你拍板时在 IM 里问你**：在「设置 → 能力」打开「允许定时任务 / 触发器 / IM 使用浏览器」（默认关闭），再把「只看页面」「点击和填写」「上传文件」「运行脚本（高级）」每类操作各设一次「允许 / 每次询问 / 拒绝」：你在场时「每次询问」是弹窗，自动任务则改为在 IM 里发审批（定时任务和触发器发到它的「结果与审批推送频道」），5 分钟没人回复即按拒绝处理。自动任务只在你设为「始终允许」的网站上操作，识别为支付、转账或政务的页面一律不动，连续被拒两次即停下，结束时在对话里留一份「浏览器任务报告」，并把结果推送到绑定的频道。设置页还能手动添加网站、预览某个网址上阿布能做什么、列出会用到浏览器的自动任务，每项浏览器设置下方也会如实显示是否保存成功。
- **浏览器自动化应付得了真实的表单、弹窗、内嵌页面和文件**：填一张表——几个输入框、点提交、等结果——可以作为一批最多 25 步一起执行，只问一次，确认框写明这批要做什么，不再每个字段问一遍。网页自己弹出的提示、确认、输入框会被报告并作答，不再把任务冻到超时；60 秒没人处理一律按「取消」，绝不自动「确定」。内嵌区域（iframe）和开放式 shadow DOM 里的输入框现在能找到、能填，每个内嵌网站单独授权（一个弹窗最多一起列 5 个）；阿布还能从你授权过的文件夹上传文件（单次最多 10 个、单个最大 20 MB），下载文件也不再弹「另存为」。批量读取页面数据改在一份只读的页面副本上进行，不再为此弹脚本审批。
- **浏览器任务守着自己的标签页，也会给你让路**：两个对话里的浏览器任务不再抢同一个内置浏览器标签页；通过「我的 Chrome」操作时，任务只在自己占用的标签页上干活，不再跟着你切过去的标签页走。切换对话或收起面板不再关掉任务打开的网页，点「停止」也会一并停下它在浏览器里的操作；你在任务正用着的页面里打字或跳转，它的下一步会等约 3 秒，即使它正在这个页面上长时间等待也一样。关掉任务的标签页等于叫它让开：在你发下一条消息之前，它不会不经询问再开新标签页。
- **任务一口气跑多少轮，由你决定**：此前任务固定跑满 200 轮就停，没有地方可改；现在「设置 → 偏好 → 最大轮次」可选 50、100、200（默认）、500、1000，自己没设上限的专家也跟随这个设置。到达上限时会出现一张卡片，提供「继续执行」和「调整上限」；点了继续又跑满时，卡片会提醒任务可能在原地打转。IM 里收到的是已经写出的部分回答，而不再是一句固定提示。
- **「能力」和「安全」设置更好读了**：「设置 → 能力」首屏只有三张卡片——阿布内置浏览器、我的 Chrome、操作电脑，各自的规则放到下一层，网站授权单独成一个列表页；所有开关的含义和取值都没变，升级后无需重设。「安全」页把默认权限模式放在最前、改成下拉选择；「操作电脑」缺哪项系统权限，就在那一项旁给一个「去授权」，直接打开对应的系统设置，回到阿布后自动重新检查。「我的 Chrome」卡片也不会在还没连过的时候误报连接已中断。
- **设置里新增「关于作者」页**：作者的联系与支持方式——公众号和「请作者喝杯咖啡」二维码（点击放大）、小红书 / X / GitHub 链接、阿布官网、版权信息和免责声明——集中到「版本」之后的这一页。「版本」页只保留版本号、设备 ID 和更新检查，「反馈」页不再放二维码，改为链接到「关于作者」。

### 🐛 修复

- **浏览器表单填入值不再泄露进诊断包**：浏览器 `fill` 工具会把写入的值（包括登录密码）原样回显在结果里，而诊断导出对工具结果全文保留，导出的诊断包因此可能带出明文密码（在一份 v0.42.0 诊断包中实证发现）。两端同修：扩展侧对自我声明敏感的字段（`type="password"`、password/OTP/`cc-*` autocomplete）只确认填入、不回显内容；诊断脱敏器对填入值做结构化脱敏——工具的 `value` 输入、结果里的回显、批量填表步骤、`previousValue`、以及修复前录下的历史对话都覆盖。脱敏器同时复用记忆卫生的凭证检测器，记忆写入门能拦下的厂商密钥形状也不会再随诊断包流出。
- **专家半途停下不再被当成做完了**：阿布派活的专家轮数用尽、被打断或出错时，停止原因现在写进派活结果正文本身（而不是只放在只有 Claude 系协议能带的 metadata 里），阿布（或团队的队长）会按未完成处理，而不是默默把活接过去自己干。批量派活的子任务行同样标注。@ 点名派活撞上轮数上限时，过去只会甩下一段被截断的答案就静默收工，现在会提示「已达到 N 轮上限，任务未完成」；主会话里原来的「已完成 N 轮执行」则换成了「新增」里介绍的轮次上限卡片。
- **沙箱拦截如实说明拦了什么**：被沙箱拒绝**执行**的命令（例如 setuid 的 `ps`）此前会被报成「写入被拦截」，还弹出指向无关目录的「授权该目录」提示。现在执行拦截单独成一类，输出里判断不出类别的拦截如实标为未分类、不再谎称写入；提示只在真的写入被拦截时弹出——读取类和未分类的拦截不再弹「授权该目录」——其余类别照旧随命令输出交给阿布。
- **阿布不再去找内置专家根本没有的文件**：阿布的能力说明此前宣称每个专家在磁盘上都有一份定义文件，于是它可能会去读某个内置专家的文件、读不到再自我纠错一通。只有你自建的专家才有文件；说明里给出的专家名单现在被明确写成权威事实源。
- **用 Windows 换行保存的专家文件能正确读取了**：专家文件用 Windows（CRLF）换行保存时，头部最后一个字段会多带一个换行符，读出来的值就不对；现在按原样读取。
- **保存专家或技能不会再覆盖、删除另一个**：新建专家或技能时用了已被占用的名字，或者把它改名成这样的名字，以前会直接覆盖原有那个的文件；改名时还会删掉被改名那一项的整个文件夹，技能附带的脚本和参考资料、专家的记忆都会一起丢失。现在编辑器会拒绝已被占用的名字（包括内置和插件提供的名字，不区分大小写），并在名字输入框下方说明；改名会把整个文件夹连同里面的内容一起移动过去；保存失败时会在「保存」按钮下方提示，而不是悄无声息。
- **阿布新建专家时不会再覆盖已有的专家**：在对话里让阿布新建专家，如果名字已被占用，以前会不声不响地把那位专家的指令整份替换掉；和内置或插件专家同名（包括阿布自己）时，还会在所有用到它的专家团里悄悄顶替它。现在只有你明确要求修改那位专家时，阿布才会用这个已被占用的名字保存，否则会换个名字或先问你。内置和插件专家的名字、只差大小写的名字，以及和专家文件里写的名字对不上的，都会被拒绝，不写入任何文件。保存前弹出的确认也会写明这次是「新建专家」还是「覆盖已有专家」。
- **编辑项目里的技能，改动会写回它原来的位置**：编辑来自项目文件夹、阿布为项目生成的技能，或共享目录 `~/.agents/skills` 里的技能时，以前会另存一份到阿布自己的目录：你自己的同名技能被悄悄覆盖，列表里显示的仍是项目里那份，改动看起来没生效。现在保存直接写回这个技能自己的文件；改名只在原处移动它的文件夹，脚本一起带走；改名中途失败会恢复原内容，再点一次保存即可。新建专家或技能时，同一时刻出现的同名文件也不会被覆盖。
- **保存专家或技能不会再擅自给文件夹改名**：文件夹名和里面的专家/技能名不一致时，以前每次保存都会顺手把文件夹改成它的名字——在项目文件夹里，这会让「只改了一句话」变成 git 里的一次文件夹改名。现在只有你真的改名，才会改文件夹名；普通保存只改文件内容，别的都不动。
- **详情页不再在中文界面里显示英文标签**：专家、技能、MCP 详情里的「来源」「描述」标签和取值、MCP 的连接出错状态，以及专家内容的「预览 / 源码」切换按钮，现在都跟随界面语言。
- **交给专家的活，现在带上你附的图片和文档**：通过 @ 点名专家派活、或让阿布自己委派时，此前只把文字交过去，专家只能反问「文件在哪」。现在附件按原来的顺序随任务一起交给专家；如果专家所用的模型读不了图片或文档，会在发出请求前就报错，而不是悄悄把附件丢掉。
- **上游内容安全拒绝不再被报成认证失败**：模型服务商的内容安全系统拒绝请求时（常由对话历史触发，例如刚压缩完上下文），阿布此前把它当成认证错误，只显示一段原始 JSON。现在错误卡片会直说是上游内容安全系统拒绝了请求、建议新开会话继续，并列出服务商返回的 HTTP 状态、错误类型和 traceId。
- **长回复流式输出时，对话不再跳动**：回复生成期间，你刚发的那条消息稳稳停在原处，不会随着思考和回答变长被推来推去——这就是 v0.40.0 留下的残余跳动。「工作过程」折叠头不再在开跑半秒后插到「思考中」上方把内容顶跳；进行中改为平滑长出一行每秒跳动的「已处理 Ns」，结束后变成「用时 Ns」。
- **发送后失败的消息不再「回来两份」**：已经发出去的消息失败时（比如服务商返回错误），对话里会留下标着失败、带「重试」的那条，输入框里还会再放回一份同样的草稿。现在只有根本没被接收的消息才会回到输入框；已被接收的消息只留在对话里，标为失败并带「重试」。
- **窄对话列里，输入框工具条保持一行**：把侧边面板拖宽后，输入框下方那排控件会断成两行，小窗口里还会互相叠在一起。现在这排控件始终单行，按固定顺序逐步收起文字（「+」和发送 / 停止键永远不动）；窗口放得下时，拖动面板也不会把对话列挤到 480 像素以下。
- **Windows 上显示出一直在用的沙箱开关**：Windows 用户在「设置 → 安全」看到的是「当前平台不支持 OS 级沙箱」，也没有开关，而命令其实一直跑在关不掉的 Windows 受限沙箱里。现在这里显示真实的开关（默认开启）和 Windows 专用说明，包括它不按文件路径隔离、网络隔离也比 macOS 弱。
- **应用刚启动时补跑的浏览器定时任务，不再丢掉浏览器工具**：阿布关着期间到期的浏览器任务，在启动后立刻补跑时，可能赶在内置浏览器就绪之前开跑，整轮都拿不到任何浏览器工具，最后还给出一张「已完成」的报告。现在会先等浏览器就绪再开跑。
- **「我的 Chrome」长截图能用了，页面太长会如实报错**：通过「我的 Chrome」截整页长图此前一次都没成功过——页面先被翻到底、等上好几秒，然后报错。现在能正确拼接；页面长到一张图装不下时（超过 Chrome 单边 65,535 像素或总面积上限）会直接报错，而不是把一张空图当成截图交给模型。
- **沙箱卡片写出真正的原因，按钮也回来了**：命令被沙箱拦下时，卡片的高亮行显示的是一个多余的「stderr:」，还可能吞掉部分输出；现在高亮的是真正的拦截原因，其余输出一行不少。沙箱恢复卡片的按钮此前渲染成了没有样式的纯文字，现在是正常按钮，说明也并成一句讲清两条路，「高级选项」挪到按钮行最右侧。
- **桌宠会记住你把它放在哪了**：以前每次启动它都回到右下角——移动时位置从来没有上报，所以没有保存，存了也恢复不了。现在拖到哪里就记在哪里，下次启动直接在原处出现，贴边半藏的位置、Retina 屏上都一样；原位置所在的显示器已经断开时，会被拉回到仍连接的屏幕上。主窗口之后再改任何设置，也不会再用自己手里的旧值把这个位置覆盖掉。
- **阿布新建技能时不会再遮住或覆盖已有技能**：在对话里让阿布新建技能，以前它只拿名字去比对当前启用、且拼写完全相同的技能，于是可能用上已停用插件里某个技能的名字、只差大小写的名字，或被待审草稿挡住的技能的名字。阿布会先于你自己的、插件和内置技能读取它新建的技能，所以在这个项目里新技能就顶替了原来那个；在 macOS 和 Windows 上还可能覆盖那个文件夹里已有的技能。现在凡是已被其他技能占用的名字（内置的、企业下发的、插件提供的（插件停用了也算）、你自己的，或只差大小写的），阿布都会拒绝，不写入任何文件；已有技能只会在阿布编辑它时才改变。
- **你打开的项目技能会一直开着，也不会再连带关掉你自己的同名技能**：项目文件夹里自带的技能（其中的 `.abu/skills` 或 `.agents/skills`），此前每次阿布重新扫描技能——启动、切换项目、技能文件夹变化——都会被再次关掉，在「扩展 → 技能」里打开也留不住。而且开关只按名字记，项目里一个和你自己的技能同名的技能，会把你那份在所有项目里一起关掉。现在阿布不会再自己改动任何技能的开关；以前被这样误关的技能，需要你手动再打开一次。
- **阿布后台进程读写文件会按要求来了**：负责执行任务的后台进程写文件时，会忽略「只新建、不覆盖」「只写已存在的文件」「追加」和文件权限这几项设置，于是本该拒绝的写入可能覆盖已有文件、把已删除的文件重新建出来，本该追加的内容则会把原文件整个替换掉；读文件时则会忽略「这个路径从哪个目录算起」的设置，读到的可能根本不是要的那个文件。目前没有功能在后台进程里用到这些设置——用到它们的保存专家、保存技能都在应用窗口里执行——所以没有文件因此丢失或读错；现在后台进程会逐项照做，做不到的设置直接报错，而不是悄悄跳过。新增的编译期检查还会拦住后台进程里其他替身实现今后再这样悄悄吞掉设置。
- **组织禁用的技能名不能再从别的入口建出来（企业版）**：组织的 Skill 黑名单以前只在「从文件夹安装技能」时检查。阿布在对话里新建技能、提议技能草稿、从 npm 或网址安装、把技能改名，以及你在技能编辑器里保存、采纳草稿、导入 .askill / .zip 技能包、在技能历史里撤销改动，都能用上被禁的名字。现在这些入口在写入任何内容之前就会拒绝，并说明是所在组织的策略不允许；删除这类技能不受影响。根目录下有两个 SKILL.md 的技能包（例如 `SKILL.md` 和 `skill.md`，在 macOS 和 Windows 上是同一个文件）现在会被拒绝——最终落盘的那一份声明的名字，可能和检查过的那一份不一样。
- **组织禁用的技能不再出现、也不能用了，不管它是怎么到电脑上的（企业版）**：Skill 黑名单以前只拦住「用被禁的名字写入技能」。已经在磁盘上、用了被禁名字的技能（项目里提交的、策略下发前装好的、插件带来的、或直接写成文件的）仍会出现在「设置 › 技能」和 / 菜单里，阿布也看得到、用得了。现在这类技能在各处都隐藏：不会列出，不能用 / 调起，阿布不能加载它、也读不到它附带的文件；阿布点名要用时，会被告知这是所在组织的策略禁用的。组织调整黑名单后，技能列表会在下一次同步策略时跟着更新，不用重启；移出黑名单的技能会自动回来。技能文件本身不会被删除，这类技能也仍然可以删掉。
- **安装后才被拉黑的组织 MCP 服务不再继续运行**：企业版里，管理员设置的 MCP 黑名单只在从「连接器 → 组织」安装服务时检查一次，更早装上的服务每次启动仍会自动连上。现在被拉黑的服务会在下一次同步策略时（5 分钟内）断开，之后不再重连；它仍留在列表里，标记为「组织已禁用」而不是「已连接」，从黑名单移除后无需重装就能恢复。你自己添加的连接器不在这份名单的管辖范围内。
- **回复刚结束就发出的消息不会再被吞**：防止重复发送的锁，要等上一条消息整条链路收尾才释放，而界面早就显示就绪了——回复渲染完、发送按钮也回来了——落在这段空窗里的消息会被直接丢掉，文字留在输入框里，只有一条小提示。从欢迎页发出去更明显：在欢迎页发起的任务，整个首轮回复期间都占着这把锁，「新建任务 → 发送」在这段时间里会整段被吞。现在落在空窗里的消息会先排队，等上一条被接收后自动发出。
- **派给专家的活不再永远停在「执行中」**：阿布后台进程发给界面的更新是成批的，只要其中一帧夹带了图片或文档的原始数据，整批更新都会被丢掉——产生它的那一步永远走不到终态，这次派活看上去就卡住了。现在只把出问题的那一帧降级（标成媒体传输失败），同批其余更新照常生效，这一步能正常结束。
- **连接器自带的界面会跟着深浅色切换了**：切换主题时，阿布只把新主题的名字发给连接器界面，没有发新的配色，界面也不会重建——于是任何采用阿布配色的连接器界面，切完主题仍是上一套颜色，自带样例的表格文字在深色下根本看不清。现在配色和主题一起发过去。
- **删掉的专家、技能或项目不会再以过期内容复活**：阿布有意对「上次读到之后已被删掉」的文件拒绝写入——给专家分配固定身份、保存项目/技能/专家时，都该拒绝而不是重新创建。桌面版此前是在写入前单独做一次「还在吗」的检查，于是恰好在这一瞬被清空的文件夹（在 Finder 里删掉，或被另一个阿布窗口删掉）照样被写回去，留下一份你已经丢掉的东西的记录。现在检查就是写入本身，中间不再有能把文件丢掉的空档。另外，指定了文件权限的写入现在会照着给，而不是落成默认权限。
- **名字是一段路径的技能或专家不再被加载**：阿布也会读取当前打开项目 `.abu/skills` 文件夹里的技能，以及别人做好、你直接放进 `~/.abu/agents` 的专家文件夹——下载来的项目或分享来的专家包可能自带这些文件——而名字同时就是它在 `~/.abu` 下的文件夹名。以前 `../../something` 这样的名字也会被接受；目前没有功能会往用这种名字拼出的路径里写东西，但按专家存取记忆的那组函数一旦被用上就会。现在这样的文件会被跳过，并在日志里留下一条点名该文件的警告；自带这种技能的插件会在安装时被拒绝；记忆函数也会拒绝任何不是单个文件夹名的名字。首尾带空格、或含不可见字符（零宽、文字方向、控制字符）的名字同样会被跳过；中文、中间的空格和点照常可用（`HR 招聘官`、`skill.v2`）。所有安装途径现在也会像编辑器那样，拒绝名字里不可见的 C1 控制字符（U+0080–U+009F）——技能和专家的名字，以及插件、插件市场和版本号都包括在内。
- **专家编辑器又能用中文名了，大小写也保留**：在编辑器里新建或改名专家时，输入会被转成小写，且只接受小写字母、数字和连字符——`财务分析师` 这种名字根本建不出来，`QA_bot` 会被悄悄变成 `qa_bot`。现在编辑器和阿布其他地方用同一套命名规则：中文等 unicode 字母和大小写都保留（空格仍会变成连字符，因为 @ 提及跨不过空格）。技能名不变，仍是小写短横线格式。

### ⚠️ 行为变更

- **自动任务默认不再使用浏览器，需要你手动打开**：v0.42.0 里，定时任务、触发器和 IM 运行可以直接操作你设为「始终允许」的网站；升级后这些运行会被拒绝，直到你在「设置 → 能力 → 内置浏览器 → 自动任务」打开总开关，任务报告也会写明是总开关没开，而不是谎报完成。
- **一个操作命中多个元素时，停下来而不是瞎猜**：页面上有两个一样的「提交」按钮这类情况，点击或填写此前会悄悄作用在第一个上；现在会停下、页面上什么都不动，并列出候选，依赖「取第一个」的任务可能需要把目标写得更具体。
- **支付、转账、政务页面每次都问，连续拒绝两次即停**：识别为支付、转账、结算或政务办理的页面，即使网站设为「始终允许」也会每次询问、且不提供「以后都允许」，自动任务则一律拒绝。连续拒绝两次浏览器操作会结束这次运行（你在场时也一样），并留言问你该怎么做。
- **无人值守运行不论怎么结束都会推送到频道，Webhook 也包括在内**：失败、停止、受阻、没有进展的运行，此前不会往任务的频道发任何消息；现在会发出结果、原因和下一步建议。Webhook 和自定义模板输出在这些情况下会收到一句简短说明和结构化的结果字段，成功时的推送内容保持不变。
- **项目技能默认打开**：项目文件夹里的技能此前默认是关着的，需要你手动打开；现在和其他技能一样默认打开，你可以在「扩展 → 技能」里关掉，关了就一直关着。
- **IM 的 webhook 监听默认只在本机。** 以前装了带心跳的连接器插件，本机的 webhook 端口就会对整个局域网开放。现在只绑本机；设置里新增一个默认关闭的开关，需要时再对局域网开放。没有启用频道的平台发来的 webhook 在解析前就丢弃，插件消息的 id 也进入防重放判断。

## v0.42.0 · 2026-08-27

### ✨ 新增

- **微信媒体双向打通**：IM 桥可以把图片和文件发给微信用户，agent 工作时显示「正在输入」状态；入站侧，一张照片和它的配文现在合并成一轮而不是拆成两条。入站图片真正送达视觉模型了（修了三个根因），入站文件和视频也是 agent 能打开的。提问和计划审批会转发给微信用户，而不是让运行卡死。
- **子代理运行开成工作区标签页**：被委派或批量派发的子代理拥有独立的 Tab（与浏览器/预览/终端同层），批次卡片里有每个子任务的实时进度（工具名、计数、状态），工具边界看得见也强制执行——工具集在启动时冻结，运行中途无法拓宽。
- **触发器有了自主能力档位**：触发器编辑器暴露只读 / 自定义 / 完全三档能力，存量记录默认只读，放行完全自主前会给警示。`manage_trigger` 不能再拓宽触发器自身的工具/命令/路径白名单。
- **工具结果图片全程可见**：截图或读文件得到的图片，任务结束后不再退化成占位文本——图片落盘快照、回放时回填、会话重开时重水化，历史内联数据会脱水以控制账本体积。
- **上下文水位分层展示**：用量指示器显示 token 花在了哪些层，而不是一个笼统的数字。
- **你自己的图片有了全窗口大图查看器**：输入框附件和已发送的用户图片缩略图，点开进入独立的全窗口查看器——可原生「另存为」保存到磁盘、用方向键在同一条消息或当前输入框附件列表内翻页、长图可用滚轮或键盘（上下/PageUp/PageDown）滚动。agent 需要审批时查看器会主动让位——权限或命令确认弹窗出现即自动关闭大图，而不是被黑幕盖住；点到置灰的翻页箭头也不会误关查看器。助手、工具、工作区的图片仍走侧边预览面板。
- **章节刻度新增鱼眼悬停**：悬停正文左侧的章节刻度会放大附近的刻度，长会话里更好定位。
- **Windows 布局整理**：Windows 下工作区头部布局正常了，标题栏不再吃掉本该落在会话上的点击。

### 🐛 修复

- **一条微信消息失败不再拖死同批其余消息**：此前单条消息处理中途抛错会跳过同一轮拉取里所有后续消息且不重试；现在每条消息独立隔离，其余消息照常到达 agent。
- **相对 `read_file` 路径不再丢失结果图片**：快照现在会回落到内联字节，而不是悄悄什么都不持久化，缩略图因此能挺过驱逐和回放。
- **更新器未启用时不再谎报「已是最新」**：检查结果三态化，没有更新器的构建不再和真正最新的构建显示同一句话。
- **终端复制与文本选区正常了**：终端标签页的选区和复制已修复，一并修了编辑框右键菜单和面板重开。
- **工作过程折叠不再隐藏回答**：收起「工作过程」只隐藏过程步骤；助手正文和中途用户消息在任何折叠状态下都可见，先叙述后干活的轮次也保留折叠能力。
- **点击停止后，命令进程树会被可靠杀掉**：sidecar 的运行归属守卫曾把中止指令静默吞掉，导致任务显示已停止、命令却仍在后台运行。中止回调现在会重入所属运行的上下文，派发失败也会记录日志而不是无声丢弃。
- **审批弹窗落在发起请求的那个会话里**：多个任务并发时，命令确认可能被盖上别的会话的标识而被过滤掉——任务会永远等一个你看不见的批准。命令确认现在与文件权限一样，携带发起方任务的标识。
- **临时目录不再在规范路径下走进死胡同**：macOS 的临时目录位于 `/var` → `/private/var` 符号链接之后，写前读检查和特权文件宿主都拒绝了自己刚批准过的路径的规范形态——临时目录下的读写全部失败，且弹出的授权永远无法通过。允许目录现在会解析到真实路径。
- **诊断包上传需要填写描述并选择会话**：反馈表单不再接受空报告——上传前必须填写问题描述并至少选择一个会话；离线导出不受限制。
- **章节刻度留在面板内**，短会话不再渲染。

### ⚠️ 行为变更

- **存量自定义触发器在工具白名单缺失或为空时改为默认拒绝。** 这类任务现在按只读档运行，不再把空的 `allowedTools` 当成不受限；只有显式配置 `allowedTools: ["*"]` 才启用工具通配。当前版本的触发器界面没有白名单编辑入口；请让管理员更新触发器配置数据，或手动修改配置，完成受影响任务的迁移。
- **定时任务现在在启动时冻结一份「当时可用工具」的快照运行**，同时作为派发过滤器和能力天花板。任务的工具集运行中途不再变化。
- **文件工具的输出与快照现在携带真实路径。** 路径获批后，会在工具执行前解析成真实位置（如 `/private/var/...`、iCloud 的真实路径），关闭审批到执行之间的空隙——所以 `list_directory` / `find_files` / `search_files` 的输出和写入快照显示的是解析后的路径，而不是你输入的写法。

## v0.41.0 · 2026-08-22

### ✨ 新增

- **图片现在真正送达 DeepSeek 的视觉模型**：适配层此前从未声明 DeepSeek 的视觉路由，附带的图片会被悄悄替换成文字占位符。支持视觉的 DeepSeek 模型现在收到的是图片本身。
- **一张超大图片不会再把会话永久卡死**：每条供应商路由在图片入口按大小和数量设预算。路由不接受的格式即使尺寸合规也会重编码——此前一张小尺寸 `.bmp` 会直接放行、进入持久历史，然后让这个会话之后的每次请求都报 400。缩放提示在重试时不再丢失，追问坐标或小字的重试轮不会再拿着缩小的截图当原尺寸读。
- **Enter 行为由你决定**：自行选择 Enter 是发送还是换行。输入法组合期间双重保护，中文/日文选字确认不会误触发送；Alt+Enter 不再打乱输入顺序。
- **浏览器工具直接操作企业后台表单**：只读组合框、antd 风格自定义下拉成为一等公民——快照看得见、点击/选择/填写直接可用，不再降级写页面脚本。等待元素消失从烧满 30 秒超时变为毫秒级返回（ref 解析不到本身就是"已消失"）；`visibility:hidden` 藏起来的弹层不再被当成活动弹层——AI 不会再从已关闭的下拉里选值。
- **长会话可以按章节跳转了**：正文左侧新增一列刻度，一轮对话一根；滚动时当前章节高亮，悬停显示该章标题和第一条回复，点击即跳转并高亮闪烁。章节完全从已有消息派生——历史会话零迁移立刻可用；窗口窄于 640px 时刻度让位，改由标题栏的历史按钮打开同一份列表。
- **侧栏会话行换上「…」菜单**：悬停不再直接露出红色删除按钮，而是「更多操作」入口；重命名、导出、移动、删除都在一个菜单里（右键不变，删除保留 5 秒撤销）。
- **获取模型列表不再暗中全选**：拉取到的模型清单默认不勾选，内置供应商也能拉取，精选清单换代到当前模型世代。获取失败的提示不再撒谎——key 被拒（401/403）此前和"该供应商不支持获取"显示同一句话，现在错误分型并带 HTTP 状态码；火山方舟的获取按钮只在真正支持列取的套餐档位显示。
- **Windows 工作区头部重排；macOS 显示「阿布」**：Windows 下工作区头部布局正常了；macOS 中文环境下应用名本地化为「阿布」。

### 🐛 修复

- **压缩过的长会话，上下文水位不再虚报**：长会话可能显示「108% 已用 · 138.4k / 128.0k tokens」，而实际发出去的请求装得下。指示器此前在重数 UI 保留的完整原始历史，把发送路径做过的压缩全部撤销了。现在它以 Agent 回合自己测出的压缩后用量为准，只额外估算仍在流式输出的那一段。显示的百分比也做了上限收敛——超过 100% 一定是度量误差。
- **带图消息的状态可靠落盘**：发送图片后，该轮的持久化状态会永远卡在"进行中"——一处 immer 草稿的浅拷贝泄漏进持久化队列，带图行的每次状态修订都静默失败，输入框还会把刚发出的内容悄悄塞回来。泄漏已修；加载和导入时会甄别"回复确实完成"的轮次（升级用户不会在历史带图轮次上看到假的"发送失败"标记）；重试/编辑/重新生成会携带图片的快照路径，不再把空图片重发一遍。粘贴复制的 `.bmp` 文件现在也和拖拽一样直接成为图片。
- **浮层弹窗盖在窗口拖拽通道上时按钮恢复可点**：`-webkit-app-region` 是操作系统级的几何并集、无视 z-index，压住拖拽通道的弹窗按钮会被静默吞掉；现在约 30 个弹窗显式豁免，并有源码扫描测试守住新增弹窗。macOS 顶部标题行恢复可拖动窗口。
- **应用内复制图片后粘贴，重新得到图片本身**：不再是无用的文件角标。
- **堵上两个回合收尾的竞态**：后续消息不会再被暂存进已结束的运行；运行迟到的清理不会再误删下一轮的崩溃恢复检查点（进程内路径与 sidecar 路径现在用同一套带归属校验的清除）。

**Full Changelog**: https://github.com/PM-Shawn/Abu-Cowork/compare/v0.40.0...v0.41.0

## v0.40.0 · 2026-08-20

### ✨ 新增

- **长对话更省钱，也更快出第一个字**：系统提示词现在跨轮稳定——时间精确到天（需要准确时间时让模型自己跑 `date`），待办、召回的记忆等每轮都在变的内容改挂在对话之后，不再塞进提示词里。配合消息历史的缓存断点，长会话不再每次请求都把整段历史按全价重算一遍。只读的 `run_command` 批次（grep、列目录、读文件）从逐个排队改为并发执行；`edit_file` 现在容忍引用原文里的空白差异，不必再为一个空格白跑一整轮重试。
- **中文记忆检索终于可用**：相关性分词此前只按空格切，一句不含空格的中文查询就是一个 token，匹配不到任何东西，召回形同虚设。现在中文按字符二元组分词，权重低于词匹配，并设了门槛——只共享一个二元组不会把无关记忆拉进来。
- **拉黑一个网站现在只需一次点击**：v0.39.0 发布了按站点的授权判定，却没有任何入口能记下「不允许」——想让某个网站别再打扰你，唯一办法反而是批准它。现在确认弹窗提供「拉黑此网站」，设置 → 能力 里也能把已允许的站点改为禁止。
- **定时任务有了自己的权限档位**：任务可以跑在与你日常聊天不同的信任级别上；始终需要询问的操作不再提供永久授权。
- **新增 `capability_snapshot` 工具**：只读地报告当前这次运行实际具备哪些能力。

### 🔒 安全

- **`read_tools` 成为真正强制的天花板**：这个无人值守的只读档位承诺「只读取信息，不做修改」，但它靠的是一个确认回调——而工作区内被判为 safe 的命令根本走不到那个回调，于是 `touch`、`mkdir`、`cp`、`node`、`npm install` 全都能执行。现在改为正向白名单，在工具清单、调度分发、sidecar 边界三处强制。任何未被明确归类的工具（包括 MCP 工具）一律拒绝。
- **委派不再能绕过档位**：以 `@某个代理` 开头的消息，以及 `delegate_to_agent` / `run_agent_batch`，此前既不传本次运行的白名单也不传黑名单——只读频道上的一条消息就能派出一个毫无天花板的子代理。三个委派入口现在都会传递两项限制，子代理侧也会强制执行。
- **Computer Use 的安全预算真正生效**：30 步 / 5 分钟的上限此前只在渲染进程侧强制，且每个批次开头都会清零，跨批次的任务永远碰不到任何一条上限。预算现在挂在主进程的任务租约上，截止时间在首次取得时就钉死，不再顺延。
- **崩溃报告只上报错误的形状，不上报内容**：自动错误上报在消息离开机器前，会把路径、网址、邮箱、中日韩文字串和引号内容归一化掉。原始报错留在本地——运行日志和你主动导出的诊断包里仍然完整。
- 写入记忆的入口会脱敏凭证形态的内容；MCP 的原始连接错误在送给模型前先做清理。

### 🐛 修复

- **停止流式回答不再丢失已经显示的部分内容**：停止时的修订写入没有排进该会话的串行持久化队列，负载下会抢在助手消息自身的落盘之前到达，找不到可修订的行，然后被静默丢弃。屏幕上看得见的部分回答会在下次加载时消失，而整条链路不会有任何报错。
- 消息历史改为只追加的账本：写入中途崩溃不会再截断对话，崩溃残留的过期快照也不会再覆盖已完成的回答或复活已删除的消息。
- 主进程和渲染进程的崩溃会记录在本地；sidecar 反复崩溃会被上报，而不是无声失败。
- Computer Use 的停止现在作用于真正持有这次运行的会话，而不是当前显示在屏幕上的那个；结构化模式下的 `get_app_state` 能正确识别前台应用。
- 思考块在「占位 → 思考中 → 已处理」之间的切换更稳：思考块改为动画展开、完成后卷起而不再重新挂载，状态行全程保持同一字号与位置；流式回答面板不再因手动吸底而抖动。这几处切换仍有少量残余位移，尚在排查，本版未完全解决。
- `delegate_to_agent` 重新被判定为可并发，扇出不会再被串行化。

### ⚠️ 行为变更

- **逐条删除消息已改为「重做一轮」。** 同类工具中没有一家提供逐条删除，它们都用回退到某一轮来回答「我想重做」这个需求，而回退也正是这套持久化账本能够保证的语义。重做非最后一轮时会先询问，并告诉你将丢弃后面多少轮。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.39.0...v0.40.0

## v0.39.0 · 2026-08-18

### ✨ 新增

- **浏览器站点授权**：浏览器操作确认弹窗新增「此网站以后都允许」，与「仅这次」并列。授权按精确站点存储（禁止 > 允许 > 询问），在 设置 → 能力 里可查看、可撤销，同时覆盖内置浏览器和已连接的 Chrome。页内脚本（execute_js）不提供永久授权——每次运行单独询问。定时任务现在可以操作你预先授权过的网站；无人在场时其余操作仍然一律拦截。
- **高风险操作审批门**：改变页面状态的浏览器自动化（点击、填表、跳转、脚本）在任何权限模式下都会先询问，因为它作用于你已登录的会话。自我扩展（创建子代理、安装 MCP 服务、改写人格）每次都需单独确认。
- **匿名报告可关闭**：设置 → 诊断 新增匿名使用/错误上报开关。

### 🐛 修复

- 关闭窗口弹窗不再被浏览器面板遮挡——开着网页标签也能正常退出应用；任何弹窗弹出时，浏览器区域显示页面冻结画面，不再闪成白板
- 浏览器工具栏悬浮提示不再被原生网页视图裁剪
- 终端面板跟随应用主题（亮色主题下不再是固定深色）
- 运行失败的错误不再重复渲染两次；余额不足的提示改为可操作的说明
- 运行被拒时（无 API Key、派发失败、确认被拒、预处理中断）用户输入不再被静默丢弃，会回填到输入框
- 工具调用意图在执行前先落盘，批次中途崩溃不会再被误判为「未执行」
- 运行时追踪事件在渲染层、sidecar、宿主三层贯穿 conversation id

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.38.1...v0.39.0

## v0.38.1 · 2026-08-16

**问题原因**：Tauri→Electron 的一次性数据迁移用旧数据库文件元数据的指纹校验「已完成」标记，而读取旧库这个动作本身就会改动其附属文件（macOS 的 SQLite WAL `-shm`、Windows 的 LevelDB LOCK/LOG），导致标记永远失效——每次启动都把旧快照重新灌回，覆盖 13 类本地数据（删掉的服务商重启后复活）。同时系统钥匙串损坏时，界面只表现为一个个 API Key 失效，看不出真实原因。

**修复**：

- 迁移完成后永久生效，不再因指纹漂移重跑；旧数据变化只记录日志，绝不静默覆盖（macOS 与 Windows 同步修复）
- 重新输入 API Key 后「无法解密」红条立即消失；加密保存失败时服务商卡片会显示警告，不再静默失败（密钥经本机兜底仍可用）
- 诊断新增「密钥加密存储」检查：实际执行写入→读回→删除探针并统计无法解密的密钥数，系统钥匙串异常一眼可见；解密失败提示改为指明加密密钥不匹配，不再猜测「更换了硬件」
- 生图：火山引擎聊天端点误配时给出可操作提示（正确生图端点 + doubao-seedream 系列），设置页同步内联警告；启动时清扫历史迁移遗留的孤儿生图密钥（macOS）

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.38.0...v0.38.1

## v0.38.0 · 2026-08-14

### 新增

- **电脑操作形成可靠闭环**：桌面控制由宿主强制执行“观察 → 动作 → 验证”。每个写操作只能使用一次与目标应用、目标进程绑定的短时观察状态；完成动作后必须重新观察，才能继续操作。
- **权限引导与任务恢复**：阿布只申请当前任务实际需要的 macOS 权限，明确提示何时必须重启；重启后可以回到原对话重新发起任务，但不会复用旧授权或旧辅助功能会话。
- **模型能力分级更清楚**：模型会显示为完整、结构化、不支持或未知。支持工具但不能读取图片的模型，包括受支持的 DeepSeek 配置，可以使用结构化辅助功能数据，同时不会被误认为具备视觉能力。

### 可靠性与安全

- 发送、删除、购买等关键操作会由 Electron 宿主重新识别并进行单次确认；原生操作结果不明确时会停止任务，不会自动重试可能已经产生的副作用。
- 过期观察、目标进程变化、Helper 重启、渲染窗口重载和重复写入都会失败关闭；Windows 也会在审批与原生输入路径之间串行执行写操作。
- 界面连续没有变化时只允许一次受控恢复；仍无进展就明确停止，不再无限循环。

### 状态与诊断

- 能力设置页和任务状态条会显示当前目标、能力模式、权限状态以及观察、动作、验证阶段。
- 本地运行轨迹可以关联对话、任务循环、工具调用、观察状态、Helper 代际和验证结果；字段使用白名单，不记录提示词、截图、辅助功能标签、用户输入或工具结果正文。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.37.4...v0.38.0

## v0.37.4 · 2026-08-14

**问题原因**：停止中的任务及其排队追问同时经过渲染层和 sidecar 的多个生命周期处理路径，可能导致队列消息在上一段回复尚未稳定结束时提前执行、状态替换后消失，或点击停止后留下空白回复。

**修复**：

- 排队追问会等待当前任务可靠进入结束状态，再严格按顺序启动，并显示与普通消息一致的“思考中”动态反馈。
- 队列消息会一直保留到自己的任务正式接管，避免追问消失或被错误拼进上一段回答。
- 停止任务会保留明确的“已停止”结果，只清理真正空白的流式占位；消息下方多余的单条“运行中”状态不再展示。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.37.3...v0.37.4

## v0.37.3 · 2026-08-13

**问题原因**：Electron Builder 26 会生成独立的更新 blockmap，但不再写入 `blockMapSize` 字段。发布暂存流程把字段缺失误判为无需上传 blockmap，导致 3 个架构的更新源虽然能下载完整安装包，差分更新元数据却返回 404。

**修复**：

- macOS Apple 芯片、macOS Intel 和 Windows 更新源现在会为每个引用产物同步发布外置 blockmap，支持差分更新的升级可以重新使用增量下载。
- 只要任一更新源引用的 blockmap 缺失，发布就会在上线前失败；更新源提供尺寸时仍会校验精确字节数。
- 无法应用差分更新的客户端仍沿用完整安装包回退，原有兜底行为不变。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.37.2...v0.37.3

## v0.37.2 · 2026-08-13

**问题原因**：Windows 凭据管理器中的记录可能在 AppData 删除后继续保留。旧 Tauri 数据已经清理时，迁移流程仍会反复读取残留或异常凭据；已完成的迁移标记也没有为后续启动保留足够的来源证据。

**修复**：

- 已无存活 Tauri 数据时，彻底清理或重新安装后不再反复报 `windows-secret-migration-failed`。
- 已完成的迁移标记会保留来源清单和可信 v2 证据，后续启动会作出一致且安全的判断。
- 只要仍有真实或无法确认的旧数据，仍保持失败关闭；不会静默丢弃可能还能恢复的凭据。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.37.1...v0.37.2

## v0.37.1 · 2026-08-13

**问题原因**：渲染窗口生命周期变化或 sidecar 临时断线时，任务可能与原有事件流脱离；即使任务已经完成或失败，消息仍会一直显示加载状态。

**修复**：

- 任务事件改用独立且带序号的 sidecar 通道，并支持状态补拉；渲染窗口重载或短暂断线后会恢复观察原任务，不再无限转圈。
- 任务结束状态只结算一次，同时隔离已经过期的 sidecar 进程，重连时不会重复执行工具操作。
- 连接异常会明确显示断开或重连状态，不再停留在含义不清的加载指示器。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.37.0...v0.37.1

## v0.37.0 · 2026-08-12

### 新增

- **跨进程任务执行更可靠**：每轮任务先写入磁盘再开始执行，通过有时限的启动确认建立运行记录；完成、失败或中断状态写入成功后才对外返回。如果 Electron 与 sidecar 断开，阿布会查询并重新接管原任务，而不是重复执行工具操作。
- **诊断包可以直接定位故障阶段**：导出前会重新检查实时状态，并加入脱敏清单与从渲染层到 sidecar 的运行时间线，可区分持久化、启动、首个响应、取消和模型服务异常；不采集提示词、回复正文、凭证或本地路径。
- **上下文预算自适应**：长对话在发送前会为系统提示、工具、图片和模型输出预留空间，再分级压缩历史，同时保留最近用户意图和关键工具结果。

### 修复

- **同一会话始终只有一个任务执行者**：连续快速发送、图片附件、定时任务、触发器和即时通讯消息都不会在同一会话中并发启动多个模型流；交互式文本追问仍可安全排队。
- **停止与崩溃恢复状态能够可靠落盘**：停止操作会等待已接收数据和本地写入完成，清理空白回复，保留排队追问；应用强退或 sidecar 换代后也能恢复已写入的部分回复。
- **自定义联网搜索状态显示准确**：配置了自定义搜索地址后，AI 服务页面不再错误显示该能力不可用。
- **更新说明跟随界面语言**：切换界面语言后，更新弹窗会同步刷新为对应的中英文版本说明，不再保留切换前的语言。

### 平台优化

- **Windows**：`py -3`、`py -3.12` 等命令会按 Python 启动器版本选择参数解析，不再把无效选择参数转交给 CPython。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.36.1...v0.37.0

## v0.36.1 · 2026-08-12

**问题原因**：用户遇到对话无法发送时，旧诊断包无法判断故障停在渲染层、Electron 主进程、sidecar 还是模型请求；版本和更新页面也可能显示过期的发布信息。

**修复**：

- 运行诊断现在通过运行标识和请求标识串联渲染层、Electron 主进程与 sidecar，记录 sidecar 就绪、首个响应、30 秒无响应、桥接确认、取消及失败等关键阶段。
- 诊断包会自动包含渲染层轨迹、待处理请求和 sidecar 状态；采集字段使用白名单并统一脱敏，不收集提示词、回复正文、凭证或模型服务商原始响应。
- 诊断页和更新检查使用应用的实时版本，主动校验更新状态，并只展示与当前发布元数据完全匹配的本地化更新说明。
- 官网下载标签及双语发布信息统一跟随最新公开的 GitHub Release。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.36.0...v0.36.1

## v0.36.0 · 2026-08-09

### ✨ 新增

- **「帮助」改为打开在线文档**：账户菜单里的「帮助」现在会按你的语言（中文 / 英文）打开官网使用指南，取代原先的应用内指南，文档更新无需升级客户端即可看到。
- **个人与组织能力统一到能力中心**：工具箱把个人能力和组织下发的能力收拢到一处，支持按能力设置作用范围，并为企业托管场景提供更清晰的授权状态。
- **面向组织的托管 Agent 模板**：企业可以下发托管 Agent 模板，与个人 Agent 并列展示，并通过约定的扩展契约保持同步。

### 🐛 修复

- **企业能力默认关闭（fail-closed）**：未授权的本地能力会被收回，授权状态同步镜像到 sidecar，组织成员不会看到自己没有权限的能力。

### 变更

- **企业客户端实现按 open-core 边界隔离**：企业网关模型接入、授权镜像与绑定流程界面完成重构，闭源逻辑留在私有模块，公开仓库只保留扩展形状。

### 文档

- 安装与使用指南改为经由官网跳转、术语统一刷新；`AGENTS.md` 成为仓库规范的唯一事实源（`CLAUDE.md` 只是 `@import` 薄壳）。

## v0.35.0 · 2026-08-05

### 新增

- **预览文件操作更集中，不再挤占标题空间**：在文件夹中显示、复制路径和另存为统一收进紧凑菜单，支持点击外部或按 Esc 关闭，适用于已支持的文件预览。
- **图片与 PDF 阅读操作更完整**：图片支持缩放、旋转、拖动和平移复位；切换工作区页签时，PDF 控件和可选择的文档内容仍保持可用。
- **工作区入口持续可见，页签状态得到保留**：尚未绑定工作区的新任务仍显示工作区选择入口，浏览器页签入口恢复；在浏览器和预览页签之间切换时，不再丢失当前预览状态。

### 安全

- **Electron 构建工具链使用已修复的 HTTP 依赖**：锁定的 `undici` 升级到 `7.29.0` 和 `6.28.0`，修复当前 High 告警及另外 7 条相关告警，不改变应用运行时行为。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.34.2...v0.35.0

## v0.34.2 · 2026-08-04

### 新增

- **未发送草稿会跟随每个任务保留**：在新建任务或已有任务中输入的文本，切换任务或重启阿布后都能恢复；草稿键按当前本地/企业账号隔离，同一台电脑切换账号不会看到上一账号的内容。文件和引用附件仍只保留在当前会话内。
- **自定义 MCP 服务支持安全重命名**：修改名称时会同步更新存储标识，以及会话和项目中的相关引用；内置服务不受影响。
- **百炼新增按量付费方案**：服务商配置中可直接选择对应方案，并提供更清晰的配置指引。

### 修复

- **Electron 恢复文件和文件夹拖拽**：macOS 与 Windows 现在都能把文件拖入对话框作为附件，也能拖入 Skill 导入弹窗；附件标签右侧留白同步优化。
- **正式安装包上传诊断包不再报 `no_console_url`**：打包阶段会校验上传地址，地址异常时给出可操作提示，发布 CI 会阻止缺少有效 Console URL 的构建上线。
- **停止任务能够稳定收尾**：渲染层与 sidecar 会统一对齐停止和完成事件，避免任务长期卡在“正在停止”或丢失最后一段结果。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.34.1...v0.34.2

## v0.34.1 · 2026-08-02

**问题原因**：网络连接准备和安装包校验阶段没有动态状态；同时候选包先编译界面、后覆盖安装包版本，导致 RC 包在界面上误显示成它仍在下载的正式版本。

**修复**：

- 更新下载现在明确展示“准备、下载、验证”三个阶段：总大小未知时显示持续移动的活动进度，传输时以一位小数实时更新百分比。
- macOS 与 Windows 发布任务使用安装包的真实候选版本编译界面，确保“关于”、诊断信息和更新器显示的已安装版本一致。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.34.0...v0.34.1

## v0.34.0 · 2026-08-01

### 重点更新

- **阿布在全部支持平台统一切换到 Electron**：macOS Apple Silicon、macOS Intel 与 Windows x64 共用同一套 Electron 外壳、阿布前端、sidecar、内置 Skills 和随包 Node/Python 运行时。三个包均在匹配架构的原生 runner 构建并执行安装态冒烟；macOS 包已完成 Developer ID 签名与公证。

### 修复

- **原生窗口操作恢复一致**：Windows 使用紧凑的系统标题栏，拖拽、最小化、最大化、关闭、菜单、弹窗遮罩、侧栏/搜索按钮和右栏缩放均可正常操作；macOS 保持紧凑红绿灯布局，可点击控件不再落入拖拽区。
- **本地 Chrome Bridge 连接更稳定**：扩展安装入口会打开正确的上一级目录，指引文案与 Chrome 文件夹选择器一致；阿布只接管能够安全识别的 bridge 进程，不会静默终止无关端口进程。
- **框架切换保留可用回退路径**：Windows 在一个版本周期内保留旧 Tauri 安装，并正确收敛重复卸载项；macOS 迁移会拒绝不安全链接、修复受支持的包管理器链接，校验失败时在显示空白数据前停止启动。
- **文档与浏览器 Bridge 依赖完成安全升级**：Excel、PPT 预览、MCP 运行时和内置 Chrome Bridge 已更新到修复已知漏洞的版本，并通过真实 Excel 读写、PPT 预览、生产依赖审计、完整构建和打包态文档/浏览器验证。
- **自动化与本地预览收紧安全边界**：自定义触发器的工具白名单现在同时作用于主进程、sidecar 和委派代理；默认应用打开不再拼接 shell 命令；HTML 预览默认阻止外网请求、表单提交和弹窗逃逸。
- **密钥写入失败不再静默丢配置**：加密存储在本次会话中异常时，阿布会立即恢复数据保全兜底，避免刚编辑的模型或辅助服务密钥在重启后消失。

### 迁移说明

- 正式 Electron 首次启动时，以已安装的 Tauri 数据作为迁移来源。阿布把会话、session、设置和受支持密钥复制到独立 Electron 数据目录，原 Tauri 数据继续保留。
- 如果机器上已有 Electron 测试数据，阿布会先生成恢复备份，再应用本次切换所需的正式 Tauri 数据；不完整的迁移记录可以重试，迁移失败会在新框架写入空状态前停止启动。
- 源码构建和 fork 包默认关闭正式数据迁移与阿布官方更新源，只有官方发布工作流可以启用。

### 已知限制

- Windows 使用当前用户级 NSIS 安装包，不需要管理员权限，但暂未进行 Authenticode 签名；SmartScreen 可能需要选择 **更多信息 → 仍要运行**。
- Chrome 扩展采用本地分发，需要在 Chrome 开发者模式下加载一次。本版本不包含 Microsoft Store 分发、完整 Windows 沙箱对齐和 Computer Use 对齐。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.33.0...v0.34.0

## v0.33.0 · 2026-07-19

### 新增

- **AI 编辑现在进预览的版本历史**：此前版本历史只记录你自己的手动保存，AI 对文件的改动不留还原点（跟直觉正好相反）。现在 AI 每次写/改文件前都会先快照文件的原状态 —— 每轮首次改动前拍一次 —— 你可以像审阅自己的改动一样查看并回退某次 AI 改动，每条都标「AI 修改前」徽标。回退时也会先自动快照当前状态，所以回退本身也能撤销。单条快照上限 5MB，seq-0 基线快照永不淘汰。

### 修复

- **打包版生成图片不再「Load failed」**：以 base64 返回的生成图片（如火山 Seedream、gpt-image-1）此前仅在安装版里显示「Load failed」—— 打包版更严格的 CSP 拦掉了 dev 版本允许的 `data:` URL 解码路径。现改用 CSP 允许的 `atob()` 解码。返回图片 URL 的后端从不受影响 —— 这也是 dev 冒烟没抓到它的原因。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.32.0...v0.33.0

## v0.32.0 · 2026-07-19

### 新增

- **HTML 预览「选择元素」→ 添加到对话**：在 HTML 预览面板点「选择元素」，悬停高亮页面上任意元素，点击即可把它作为引用发进输入框（可附一句评论作为指令）。拾取脚本由本机 loopback 预览服务器在服务端注入，选中结果经 `origin` + nonce 校验的 `postMessage` 回传，只在你自己的本地文件上运行。悬停标签、点击切换选中、评论快捷键（⌘/Ctrl+J）都对齐文档选区工具条。
- **更新日志按语言本地化**：应用内更新弹窗与官网「What's new」现在按你的语言（中文 / 英文）显示发版说明，每次发版由单一结构化来源驱动。

### 变更

- **本地 HTML 走预览面板**：隐藏手动新建浏览器页签的入口；本地 HTML 文件统一在预览面板查看 —— 现在也能在这里选择元素 —— charset（UTF-8）与源码切换本就在那里。浏览器页签仍可通过程序打开，留作未来的 agent 浏览舞台。

### 修复

- **Markdown**：单个波浪号 `~` 不再渲染成删除线（仅 `~~x~~` 才划删）。
- **UI**：凹陷画布上选中/激活项现在可见；滚动条 app 级隐藏、滚动时才显示；更新下载进度条不再出现异常的宽度动画。
- **主题**：Windows 暗色模式下原生标题栏跟随应用主题；升级用户的默认主题会在本次更新时一次性重置为亮色。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.31.0...v0.32.0

## v0.31.0 · 2026-07-18

### 新增

- **右栏多页签工作区**：右栏文件预览升级为多页签工作区 —— 多文件预览并存（keep-alive 隐藏不卸载）、真 PTY 终端（portable-pty 后端 + xterm.js 前端）、原生 webview 浏览器页签（任意站点，不受 iframe 的 X-Frame-Options 限制），并以「任务摘要」作为默认页签。
- **主窗口卡片化改版**：卡片化视觉层次、顶栏与 macOS 红绿灯对齐、工具箱与自动化收进主布局的卡片网格（固定尺寸横卡 + 自适应铺满 + 统一启用开关）。
- **会话全文搜索接入 FTS5**：侧栏会话搜索弹窗接入 SQLite FTS5，可按标题 + 正文跨历史会话检索。
- **账户菜单 + 设置导航整理**：左下角三个按钮收进头像 popover（内联切主题/语言、接真实检查更新流），设置分组重组。

### 变更

- **字号排版体系（8-token）**：全部字号迁移到 8 档 `--text-*` token（font-size + line-height + font-weight 三绑定），清零 px 硬编码与命名字号；阅读正文号定为 14px（对齐主流聊天客户端），标题字重封顶 600。
- **语义色 + 链接色 tokenization**：链接与状态色收敛为 `--abu-{danger/warning/success/info}`（fg/solid/bg 三角色）+ 专用 `--abu-link`（品牌橙不复用 accent）；765 处裸 Tailwind 色阶转 token 并达 WCAG AA，`--abu-text-muted` 也提到 AA。

### 修复

- **消息列表底部锁定 + 搜索跳转**：虚拟化消息列表在打开/切换会话时正确锁定到底部；搜索命中跳转配淡出高亮。
- **`cn()` 吞字号 token**：修复 tailwind-merge 把 `text-[var(--)]` 误判为 font-size 从而吞掉字号 token 的问题（extendTailwindMerge 根治，app 级）。
- **工作区页签交互**：页签拖拽真正重排 + 中性插入线、关闭（×）不再误触发拖拽、PDF worker 经 Vite `?url` 加载并 memoize file 对象（修「object can not be cloned」）、恢复面板收起按钮。
- **PTY 子进程回收**：终端被杀后回收子进程，spawn 竞态下清理孤儿进程。
- **macOS 顶栏可点性**：页签条上方的 + 按钮曾落在 macOS 拖拽区导致不可点，已修。

**完整变更**：https://github.com/PM-Shawn/Abu-Cowork/compare/v0.30.0...v0.31.0
