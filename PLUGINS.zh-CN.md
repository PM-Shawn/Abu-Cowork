# 个人插件：创建、使用与维护

插件把一组技能、代理和 MCP 连接器组合成一个可独立启停的安装单元。创建插件不需要先创建插件市场。

## 创建自己的插件

1. 打开「扩展 → 插件」，点击右上角「添加 → 创建插件」。
2. 在创建对话中描述要解决的工作。阿布会使用内置 `abu-plugin-builder` 技能，在该对话的专用工作目录生成文件。生成中断后可以从「我的」继续编辑。
3. 生成后运行校验。也可以在「我的」打开草稿，点击「校验并预览」。校验不会启动包里的 MCP，也不会安装插件。
4. 阅读安装预览中的技能、代理、连接器命令或地址以及不支持的内容。需要凭据时，在预览的密码输入框填写；不要在对话里发送凭据。
5. 确认安装后，插件出现在「我的」，可以启停、查看内容和立即试用。

源码位于 `~/Abu Plugins/<作者 ID>/`。「继续编辑」重新打开创建对话；「显示源码」在系统文件管理器中定位目录。作者身份由本机创建记录确定，市场安装项不会因为来源是本地目录就变成原创插件。

编辑源码不会立即改变正在使用的安装版本。修改完成后，创建对话会校验最新内容并说明下一步；“我的”在已校验内容与安装版本不同时显示“可更新”。打开插件详情，点击底部「预览更新」并确认「更新」，才替换运行快照。若修改后尚未校验，可以先点击详情底部「检查更新」；没有变化时会在弹窗内明确提示。即使版本号不变，内容变化也能识别。已经关闭的插件更新后继续保持关闭；已有子能力的关闭偏好保留；新增能力或执行配置变化的连接器需要单独开启。

「卸载」移除运行版本及其拥有的登记，保留作者记录、创建会话和源码，之后可以重新安装。未再被使用的插件专用凭据会在事务完成后清理；其他账号授权不会因此删除。

## 添加插件市场

「添加插件市场」用于接入一个插件目录索引，便于浏览和安装其中的插件。它不会把这些插件变成你的原创，也不是创建插件的前置条件。

当前入口选择本机已有的市场目录，目录内需要存在有效的市场清单。可以接入已经下载或克隆到本机的市场。市场里的远程 Git 条目仍使用现有的远程获取、固定版本和预览流程；这个入口本身不新增公共发布服务。

同一市场重复添加会选中已有来源。同名但不同目录的来源会提示冲突。刷新失败保留上次可读列表，并停止从该旧列表安装，修复来源后可重试。移除市场仅移除索引，已安装插件仍可在来源不可用的分组中管理和卸载。

本期不包含手动上传插件包、导入/导出归档、分享或公开分发。

## 包格式

推荐使用 `.abu-plugin/plugin.json`：

```json
{
  "name": "writing-helper",
  "version": "1.0.0",
  "description": "整理笔记并形成文章",
  "skills": ["./skills"],
  "mcpServers": "./.mcp.json"
}
```

`name` 使用稳定的英文小写名称，例如 `writing-helper`。首次校验通过后，本机作者记录绑定该名称；后续修改保持名称不变。

- 技能：`skills/<技能目录>/SKILL.md`，YAML 头部至少声明 `name`、`description`，后面是 Markdown 指令。额外技能目录可通过 `skills` 字符串或数组声明。
- 代理：`agents/<名称>.md`，声明 `name`、`description` 和非空指令正文。安装预览会说明名称冲突、保留名称或空正文导致的跳过。
- MCP：可以内联在 `mcpServers` 对象，或使用包内配置文件引用；独立 `.mcp.json` 支持 `mcpServers` / `mcp_servers` 外层对象。多处来源重复声明同一个连接器名称会报错，请保留一处定义。
- 兼容清单候选按 `.abu-plugin`、`.claude-plugin`、`.codex-plugin` 顺序读取。目录名兼容不代表支持对应产品的全部能力。`hooks`、`commands` 等未支持内容会在预览中说明，不执行。
- 声明的文件路径必须留在包内。不能通过 `..`、绝对路径或链接读取包外内容；链接不会安装成可执行能力。

MCP 名称最长 128 字符，使用字母、数字、下划线和连字符；保留对象属性名不可使用。

每个 MCP 必须选一种连接方式，不能同时声明命令和 URL：

```json
{
  "mcpServers": {
    "example-service": {
      "command": "npx",
      "args": ["-y", "your-mcp-package"],
      "env": { "API_TOKEN": "${config.API_TOKEN}" }
    }
  }
}
```

这是格式示例，请替换为实际信任的 MCP 包。stdio 支持 `command`、`args`、`env`；HTTP 支持 `url`、`headers`。可选 `transport` 为 `stdio` 或 `http`，必须与声明匹配。

HTTP 配置示例：

```json
{
  "mcpServers": {
    "example-http": {
      "url": "https://example.com/mcp",
      "headers": { "Authorization": "Bearer ${config.API_TOKEN}" }
    }
  }
}
```

`${config.NAME}` 只用于 `env` 或 `headers`，不能放入命令、参数或 URL。名称以字母开头，后续是字母、数字或下划线，最多 64 字符。每个插件最多 32 个配置字段；单值最多 16 Ki 字符，序列化总量最多 64 KiB。配置值通过 Electron 的系统加密存储保存，运行设置只持有引用；源码、安装预览摘要和创建工具返回值不包含这些输入值。缺失配置不会启动连接器。无法提供系统加密存储时，保存会失败，不退回明文。

不要假设其他产品的所有环境变量或根目录占位符都在 Abu 可用。未解析的变量会阻止插件 MCP 启动；使用有效的本机命令、包管理器启动方式或明确支持的配置字段。

## 更新与故障恢复

预览基于宿主捕获的固定内容。确认过程中修改源码，不会替换已经审核的内容。安装、更新、卸载在同一宿主写入通道执行，旧快照和操作记录保留到恢复完成。意外退出后，启动时先恢复未完成事务，再开放能力。

恢复失败时页面会给出错误和重试入口，相关能力保持关闭。修复来源或配置后重试；不要手动覆盖安装登记或删除恢复记录来绕过错误。
